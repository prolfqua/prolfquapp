% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactive_report_plots.R
\name{volcano_ggiraph}
\alias{volcano_ggiraph}
\title{Interactive volcano plot with contaminants marked (ggiraph)}
\usage{
volcano_ggiraph(
  data,
  effect = "diff",
  score = "FDR",
  contrast = "contrast",
  id_cols = "protein_Id",
  fc_threshold = 1,
  score_threshold = 0.05,
  contaminant_col = "CON",
  colour_col = "estimate_type",
  width_svg = 9,
  height_svg = 7
)
}
\arguments{
\item{data}{annotated contrast data.frame}

\item{effect}{name of the effect-size column (e.g. \code{"diff"} or
\code{"log2_EFCs"})}

\item{score}{name of the score column plotted as \code{-log10} (e.g.
\code{"FDR"} or \code{"BFDR"})}

\item{contrast}{name of the contrast (facet) column}

\item{id_cols}{hierarchy-key column(s) united into the point label / tooltip}

\item{fc_threshold}{effect-size threshold (green vertical ablines); \code{NULL}
to omit}

\item{score_threshold}{score threshold (red horizontal abline at
\code{-log10(score_threshold)}); \code{NULL} to omit}

\item{contaminant_col}{name of the logical contaminant flag column
(default \code{"CON"}); ignored if absent}

\item{colour_col}{name of the point colour column (default
\code{"estimate_type"}); ignored if absent (single colour)}

\item{width_svg, height_svg}{girafe canvas size in inches}
}
\value{
a \code{ggiraph::girafe} interactive htmlwidget
}
\description{
Builds an interactive volcano from an annotated contrast table (as produced by
\code{DEAnalyse$get_annotated_contrasts()}). Points are coloured by
\code{colour_col} (e.g. \code{estimate_type}); contaminants (rows where
\code{contaminant_col} is \code{TRUE}) are drawn as triangles so they are
visually flagged while kept in the plot. Rows with a missing effect or score
(e.g. decoys, which carry NA statistics) are dropped, so decoys never appear.
Hovering a point shows its identifier and statistics.
}
\examples{
contr <- data.frame(
  protein_Id = c("sp|P1|X", "sp|P2|X", "zz|CON1|X"),
  contrast = "A_vs_B",
  diff = c(1.2, -0.3, 2.1),
  FDR = c(0.001, 0.5, 0.2),
  estimate_type = "observed",
  CON = c(FALSE, FALSE, TRUE)
)
if (requireNamespace("ggiraph", quietly = TRUE)) {
  g <- volcano_ggiraph(contr, id_cols = "protein_Id")
  stopifnot("girafe" \%in\% class(g))
}
}
\seealso{
Other reporting: 
\code{\link{intensity_density_ggiraph}()}
}
\concept{reporting}
