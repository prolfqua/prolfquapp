% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_h5ad_atomic.R
\name{write_h5ad_atomic}
\alias{write_h5ad_atomic}
\title{Write an AnnData object atomically, validating the round trip}
\usage{
write_h5ad_atomic(
  adata,
  path,
  validate = function(restored) NULL,
  compression = "gzip"
)
}
\arguments{
\item{adata}{An `anndataR` AnnData object.}

\item{path}{Destination `.h5ad` path. Its directory must exist.}

\item{validate}{Function of one argument, the restored AnnData. Should
`stop()` on a failed round trip; its return value is ignored.}

\item{compression}{Passed to `write_h5ad()`. Default `"gzip"`.}
}
\value{
The normalized destination path, invisibly.
}
\description{
Writes `adata` to a temporary `.h5ad` in the destination directory, reads
it back with [anndataR::read_h5ad()], calls `validate` on the restored
object, and renames the temporary file to `path` only if validation
returns. The temporary file is removed on any failure.
}
