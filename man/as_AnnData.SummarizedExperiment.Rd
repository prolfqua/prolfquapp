% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_AnnData_SummarizedExperiment.R
\name{as_AnnData.SummarizedExperiment}
\alias{as_AnnData.SummarizedExperiment}
\title{Convert a SummarizedExperiment to AnnData}
\usage{
\method{as_AnnData}{SummarizedExperiment}(
  x,
  x_mapping = NULL,
  layers_mapping = TRUE,
  obs_mapping = TRUE,
  var_mapping = TRUE,
  obsm_mapping = TRUE,
  varm_mapping = TRUE,
  obsp_mapping = TRUE,
  varp_mapping = TRUE,
  uns_mapping = TRUE,
  assay_name = NULL,
  output_class = c("InMemory", "HDF5AnnData", "ReticulateAnnData"),
  ...
)
}
\arguments{
\item{x}{a \code{SummarizedExperiment}}

\item{x_mapping}{name of the assay to use as `X`; an alias for
\code{assay_name} kept for compatibility with the generic}

\item{layers_mapping, obs_mapping, var_mapping, obsm_mapping, varm_mapping, obsp_mapping, varp_mapping, uns_mapping}{accepted for signature compatibility with \code{anndataR::as_AnnData};
this method always maps every slot it can and only \code{TRUE} is
supported}

\item{assay_name}{assay used as `X`; defaults to the last assay}

\item{output_class}{must be \code{"InMemory"}; write the result out with
\code{write_h5ad()} instead of asking for another class}

\item{...}{ignored}
}
\value{
an AnnData object
}
\description{
Registers prolfquapp's `SummarizedExperiment` method on
\code{anndataR::as_AnnData}. anndataR itself only provides methods for
`SingleCellExperiment` and `Seurat`.
}
\details{
The conversion is mechanical: assays become layers, `colData` becomes `obs`,
flat `rowData` columns become `var`, nested `rowData` data frames become
`varm` matrices, and `metadata` is carried into `uns$prolfquapp` in full.
Because metadata is passed through rather than enumerated, anything recorded
on the SummarizedExperiment reaches the AnnData without changing this code.
}
