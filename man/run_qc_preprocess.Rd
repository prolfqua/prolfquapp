% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmd_helpers.R
\name{run_qc_preprocess}
\alias{run_qc_preprocess}
\title{Preprocess quantification data for QC reporting}
\usage{
run_qc_preprocess(
  indir,
  dataset,
  software,
  yaml_file = NULL,
  outdir = "qc_dir",
  project = "",
  order = "",
  workunit = ""
)
}
\arguments{
\item{indir}{directory containing quantification output files}

\item{dataset}{path to annotation CSV/TSV/XLSX}

\item{software}{software key (e.g. "DIANN", "SIM"); looked up in
\code{\link{prolfqua_preprocess_functions}}}

\item{yaml_file}{optional path to config YAML; if it exists, used
instead of generating a default config}

\item{outdir}{output directory (used only when no yaml_file)}

\item{project}{project ID (used only when no yaml_file)}

\item{order}{order ID (used only when no yaml_file)}

\item{workunit}{workunit ID (used only when no yaml_file)}
}
\value{
list with \code{xd} (preprocessed data), \code{files}
  (discovered file paths), and \code{config} (ProlfquAppConfig)
}
\description{
Resolves configuration (from YAML or defaults), reads the annotation,
and runs software-specific preprocessing. Returns everything needed
to construct a \code{\link{QC_generator}}.
}
