# TODO: Compact Placeholder Figures in DEA Report

Status: implemented in `vignettes/Grp2Analysis_V2_R6.Rmd`.

## Context

`pd_metaboanalyst/Grp2Analysis_V2_R6.Rmd` and `pd_metaboanalyst/DiffExpQC_R6.Rmd` are working copies generated by the
`prolfqua_dea_cd.sh` workflow and are ignored by `pd_metaboanalyst/.gitignore`.

The runtime path is:

1. `prolfqua_dea_cd.sh`
2. `prolfquapp/inst/application/CMD_DEA_CD.R`
3. `prolfquapp::copy_DEA_R6_Files()`
4. installed `prolfquapp/doc/Grp2Analysis_V2_R6.Rmd`

In source control, that installed `doc/` file is generated from:

- `prolfquapp/vignettes/Grp2Analysis_V2_R6.Rmd`

The source template already keeps unavailable plots as placeholders through `plot_or_empty()`, but the affected chunks
use full plot heights, making the placeholders much taller than needed when their plot object is `NULL`.

Affected chunks:

- `nrPerSample2`
- `naHeat`

## Plan

1. Change only the tracked source template `vignettes/Grp2Analysis_V2_R6.Rmd`.
2. Keep the normal figure heights unchanged when plot objects are available.
3. Make fallback chunks use a compact height only when their plot object is `NULL`, for example with conditional chunk
   options such as `fig.height=if (is.null(nah)) 1.2 else 6`.
4. Do not edit ignored/generated copies in `pd_metaboanalyst`, `prolfquapp/doc/`, `prolfquapp/tests/testthat/`, or
   `prolfquapp/inst/application/CompoundDiscovery/`.
5. Verify the conditional chunk option with a focused knitr render. Full report rendering is optional because it depends
   on a complete DEA result object.
