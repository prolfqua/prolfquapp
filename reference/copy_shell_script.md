# copy shellscript to run the DEA app

copy shellscript to run the DEA app

## Usage

``` r
copy_shell_script(workdir = getwd())
```

## Arguments

- workdir:

  directory where to copy file - default is current working directory.
