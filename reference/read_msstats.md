# read MSstats.csv files and rollup to ProteinSequence level.

read MSstats.csv files and rollup to ProteinSequence level.

## Usage

``` r
read_msstats(file)
```

## Arguments

- file:

  path to MSstats csv file
