# Detect subset columns in a CompoundDiscoverer long export

Detect subset columns in a CompoundDiscoverer long export

## Usage

``` r
get_CD_subset_columns(long_file)
```

## Arguments

- long_file:

  path to \*\_long.csv

## Value

character vector of columns after Group
