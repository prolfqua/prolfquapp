# copy dockerfile to run the DEA app

copy dockerfile to run the DEA app

## Usage

``` r
copy_docker_script(workdir = getwd())
```

## Arguments

- workdir:

  directory where to copy file - default is current working directory.
