# read dataset file in csv, tsv or xlsx format

read dataset file in csv, tsv or xlsx format

## Usage

``` r
read_table_data(file_path)
```

## Arguments

- file_path:

  path to csv, tsv, or xlsx file
