# get petpide.txt and fasta file location in folder

get petpide.txt and fasta file location in folder

get petpide.txt and fasta file location in folder

## Usage

``` r
get_MQ_peptide_files(path)

get_MQ_peptide_files(path)
```

## Arguments

- path:

  path to data directory

## Value

list with paths to data and fasta

list with paths to data and fasta
