# Discover simulated data files (no real files needed)

Returns placeholder paths. Used by the SIM preprocessor for testing CMD
scripts without real quantification data.

## Usage

``` r
get_SIM_files(path)
```

## Arguments

- path:

  ignored

## Value

list with `data` and `fasta` placeholder strings
