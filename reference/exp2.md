# transform lfq data with x^2 - apply if non log data is needed

transform lfq data with x^2 - apply if non log data is needed

## Usage

``` r
exp2(lfqTrans)
```

## Arguments

- lfqTrans:

  transformed LFQData
