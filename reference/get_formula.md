# will generates formula

will generates formula

## Usage

``` r
get_formula(config, interaction = FALSE)
```

## Arguments

- config:

  AnalysisConfiguration object

- interaction:

  if TRUE include interaction terms
